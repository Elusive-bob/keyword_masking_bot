# Keyword Masking Bot

Telegram bot for group chats that masks configured keywords and reposts a censored message.

Python 3.14.3

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```
